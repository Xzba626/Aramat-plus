# C1+C2 re-proof — 2026-08-03

## Fixes
- **C1:** `requireStoreAccess` on all `/api/stores/[id]/*` children; sales/transfers/reservations/return-in/revisions scoped; notifications dash chips use `scopedStoreId`.
- **C2:** `hardDeleteProductCascade` refuses delete when `saleItem`/`transferItem` exist (`PRODUCT_HAS_HISTORY`); archive TTL purge skips those products.

## Evidence

| Check | Result |
|-------|--------|
| `npx tsx scripts/zt-c2-purge-history-proof.ts` | **PASS** `{ ok: true, c2: "PASS" }` |
| `npx tsx scripts/zt-final-cert-idor2.ts` | **PASS** fail=0 — foreign store parent+children **403**; own store **200**; users/journal **403**; packaging cost PATCH **403** |
| `npx tsx scripts/zt-page-smoke.ts` | **PASS** Owner / Manager / Seller routes + redirects |
| Browser MCP → `127.0.0.1` / LAN | **BLOCKED** (`chrome-error`) — IDE browser cannot reach local Next; HTTP smoke used as role walkthrough substitute |

## Remaining (not C1/C2 — High from prior cert)
- Warehouse company-wide reads (shared WH by design?)
- Export cost visibility to manager
- Packaging `?? 1` fallback
- No OWNER_DIRECT recreate after wipe
- Gift rules UI without POS apply

## Verdict for commercial question
**C1+C2 closed with HTTP proof.** Safe to reopen commercial-readiness discussion; High items above still open.
